// 🔥 Debouncing
// 짧은 시간 간격으로 연속해서 이벤트가 발생하면 이벤트 핸들러를 호출하지 않다가 마지막 이벤트로부터 일정 시간(delay)이 경과한 후에 한 번만 호출하도록 하는 것
// 입력값 실시간 검색, 화면 resize 이벤트 등에서 사용
// 서버에 대한 불필요한 API 호출을 줄이고 UI가 급격한 수의 이벤트를 처리하는 것을 방지하기 위해 사용
